# =============================================================================
# FINANCIAL COMPASS - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git!

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION (Public - Safe to expose in browser)
# -----------------------------------------------------------------------------
# These are prefixed with VITE_ and are embedded in the frontend build
# They are visible to anyone inspecting your website

# Supabase Configuration (PUBLIC KEYS - Safe)
VITE_SUPABASE_URL=https://evbijbadhkeorxtkymxk.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key-here

# Backend API URL (PUBLIC - Safe)
VITE_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# NOTE: All VITE_ prefixed variables are PUBLIC and will be in your JS bundle
# Do NOT put secrets in VITE_ variables!
# -----------------------------------------------------------------------------
