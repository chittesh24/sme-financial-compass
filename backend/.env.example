# =============================================================================
# BACKEND ENVIRONMENT VARIABLES - SECRETS
# =============================================================================
# Copy this file to .env and fill in your actual SECRET values
# NEVER commit backend/.env to git!

# -----------------------------------------------------------------------------
# SECURITY SECRETS (⚠️ CRITICAL - Must be kept secret!)
# -----------------------------------------------------------------------------
SECRET_KEY=your-super-secret-key-min-32-characters-change-this-in-production
ENCRYPTION_KEY=your-encryption-key-for-banking-tokens

# -----------------------------------------------------------------------------
# DATABASE SECRETS (⚠️ CRITICAL - Contains admin access!)
# -----------------------------------------------------------------------------
# Supabase Service Role Key - Has admin access, NEVER expose to frontend!
SUPABASE_URL=https://evbijbadhkeorxtkymxk.supabase.co
SUPABASE_KEY=your-supabase-anon-key-here
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-here-KEEP-SECRET
DATABASE_URL=postgresql://postgres:[password]@db.evbijbadhkeorxtkymxk.supabase.co:5432/postgres

# -----------------------------------------------------------------------------
# API KEYS - SECRETS (⚠️ Keep these private!)
# -----------------------------------------------------------------------------
# OpenRouter API - For LLM/AI features
OPENROUTER_API_KEY=your-openrouter-api-key

# Banking APIs - Contains sensitive access credentials
PLAID_CLIENT_ID=your-plaid-client-id
PLAID_SECRET=your-plaid-secret
PLAID_ENV=sandbox

RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-secret

# GST API (Optional)
GST_API_KEY=your-gst-api-key
GST_API_URL=https://gst-api-url

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS (Public - Safe)
# -----------------------------------------------------------------------------
DEBUG=false
PORT=8000

# Redis (Optional - for caching)
REDIS_URL=redis://localhost:6379
